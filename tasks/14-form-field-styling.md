# Task 14: Form Field Styling Polish

Update the input fields, buttons, and selects across all module forms to match the polished design.

## Requirements

- Standardize UI elements across `MatchModuleForm`, `SentenceModuleForm`, and `ConversationModuleForm`:
  - Inputs/Textareas: Subtle border, rounded corners, consistent focus states.
  - Selects: Consistent styling with the inputs.
  - Buttons:
    - "+ Add Pair", "+ Add Tile", "+ Add Step": Outline buttons with blue text and icon.
  - Labels: All-caps, small, muted text above fields where applicable.
- Ensure proper spacing (margins/padding) between form elements within the module cards.

## Files to Modify

- `src/components/MatchModuleForm.tsx`
- `src/components/SentenceModuleForm.tsx`
- `src/components/ConversationModuleForm.tsx`
- `src/components/VocabFindCreate.tsx` (for the search input)

## Context

The goal is to move away from default browser styling to a premium, custom-styled look as seen in the reference image.
